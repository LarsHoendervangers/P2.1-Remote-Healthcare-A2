<Window x:Class="RemoteHealthcare_Dokter.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RemoteHealthcare_Dokter.Views"
        xmlns:viewmodels="clr-namespace:RemoteHealthcare_Dokter.ViewModels"
        mc:Ignorable="d"
        x:Name="Login"
        Title="LoginView" Height="250" Width="350">
    <Window.DataContext>
        <viewmodels:LoginViewModel/>
    </Window.DataContext>
    <Grid>
        <Label Content="Username" HorizontalAlignment="Center" Margin="0,-80,0,0" VerticalAlignment="Center" Width="180"/>


        <Label Content="Password" Margin="0,30,0,0" VerticalAlignment="Center" HorizontalAlignment="Center" Width="180"/>
        <PasswordBox Name="PassBox" Margin="0,70,0,0"  VerticalAlignment="Center" HorizontalAlignment="Center" Width="180" PasswordChanged="PassBox_OnPasswordChanged"/>

        <Button Content="Log in" HorizontalAlignment="Center" Margin="0,140,0,0" VerticalAlignment="Center" Command="{Binding Path=CloswWindowCommand}" CommandParameter="{Binding ElementName=Login}"/>
        <TextBox HorizontalAlignment="Center" Margin="0,-40,0,0" Text="{Binding UserName}" TextWrapping="Wrap" VerticalAlignment="Center" Width="180" Height="17"/>

    </Grid>
</Window>
