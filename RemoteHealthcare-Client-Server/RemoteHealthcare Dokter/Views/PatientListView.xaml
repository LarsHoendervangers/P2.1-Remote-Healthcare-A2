<UserControl x:Class="RemoteHealthcare_Dokter.Views.PatientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RemoteHealthcare_Dokter.Views"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1518">
    <Grid>
        <Label Content="Patienten" HorizontalAlignment="Left" Margin="28,10,0,0" VerticalAlignment="Top" FontWeight="Bold" FontSize="36"/>
        <DataGrid Margin="28,76,32,67" SelectionChanged="DataGrid_SelectionChanged" GridLinesVisibility="Horizontal" BorderBrush="#FFC6002A" ColumnHeaderHeight="NaN" FontSize="20" ItemsSource="{Binding PatientsList}">
            <DataGrid.ColumnHeaderStyle>
                <Style/>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.CellStyle>
                <Style/>
            </DataGrid.CellStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Naam" Width="*"/>
                <DataGridTextColumn Header="ID" Width="*"/>
                <DataGridTextColumn Header="Geslacht" Width="*"/>
                <DataGridTextColumn Header="Leeftijd" Width="*"/>
                <DataGridTextColumn Header="Geboortedatum" Width="*"/>
                <DataGridTextColumn Header="Laatste bezoek" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
